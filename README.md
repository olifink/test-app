# README

demo app for `Error: You don't have permission to do that.` problem

## dev mode works

```bash
yarn rw dev
```

log in with `fred` / `hello1234!`

## serve mode shows error

```bash
yarn rw build
yarn rw serve
```
